/**
 * @interface IPropsCard
 * @description The properties for the card component.
 * @props `title` `string` Sets the title of the card
 * @props `height` `number` Sets the maximum height of the card
 */
export interface IPropsCard {
    title?: string
    height?: number
}
