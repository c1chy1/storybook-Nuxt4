/**
 * @interface IPropsViewportHint
 * @description Defines properties for the viewport hint component.
 * @props `description` `undefined` Sets the description of the viewport hint
 * @props `headline` `undefined` Sets the headline of the viewport hint
 * @props `subheadline` `undefined` Sets the subheadline of the viewport hint
 */
export interface IPropsViewportHint {
    description?: string
    headline?: string
    subheadline?: string
}
