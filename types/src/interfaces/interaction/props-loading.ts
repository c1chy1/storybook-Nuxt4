/**
 * @interface IPropsLoading
 * @description Properties for the Loading component
 * @props `loading` - Whether the loading state is active
 */
export interface IPropsLoading {
    loading?: boolean
}
