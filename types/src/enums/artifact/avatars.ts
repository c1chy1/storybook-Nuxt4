/**
 * @enum {string} EAvatars
 * @description Defines types used for the avatar component.
 * @prop AVATAR Represents an avatar with a provided image
 * @prop INITIALS Accepts characters as an avatar instead of an image
 */
export enum EAvatars {
    AVATAR = 'avatar',
    INITIALS = 'initials',
}
