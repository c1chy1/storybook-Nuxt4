/**
 * @enum {string} ESteps
 * @description Defines types used for the step component.
 * @prop DEFAULT The step layout is shown without numbers
 * @prop NUMBERED The step layout is shown with numbers
 */
export enum ESteps {
    DEFAULT = 'default',
    NUMBERED = 'numbered',
}
