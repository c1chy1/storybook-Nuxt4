/**
 * @enum {string} EInputVariants
 * @description Defines variants used for the input component.
 * @prop DEFAULT Default input style with a standard appearance
 * @prop PRIMARY Input styled with primary brand colors, suitable for primary actions
 */
export enum EInputVariants {
    DEFAULT = 'default',
    PRIMARY = 'primary',
}
